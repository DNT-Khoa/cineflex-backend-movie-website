stages:
  - build
  - test
  - deploy

build:
  stage: build
  script: ./mvnw compile

test:
  stage: test
  script: ./mvnw test

deploy:
  stage: deploy
  image: ruby:latest
  script:
    - apk update
    - apk add ruby-dev
    - gem install dpl
    - dpl --provider=heroku --app=$HEROKU_APPNAME --api-key=$HEROKU_APIKEY
  only:
    - master
